-- let-polymorphism vs lambda-bound monomorphism
let id = \x. x in id id
let id = \x. x in if id true then id 1 else id 2
\f. if f true then f 1 else f 2
let const = \x y. x in const
let twice = \f x. f (f x) in twice twice
let compose = \f g x. f (g x) in compose compose
